# Install application icon
install_data(
  'me.rueegger.bootmate.svg',
  install_dir: iconsdir / 'hicolor' / 'scalable' / 'apps'
)

# Install symbolic icon
install_data(
  'me.rueegger.bootmate-symbolic.svg',
  install_dir: iconsdir / 'hicolor' / 'symbolic' / 'apps'
)
